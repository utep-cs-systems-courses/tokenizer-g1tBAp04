#include "history.h"
#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "tokenizer.h"

int main(){
  printf("/n$ >");
  char user_input[101];
  fgets(user_input,100,stdin);

  //history nput
  if(user_input[01[ == 33 && (user_input[1] > 48 && user_input[1] < 58)){

	printf("%s\n","History Log");
	

	
      }
}
